# TcpProxy-
Secure TCP proxy with TLS/SSL using asio. The client authenticates with the server, establishes a connection pool, and handles commands. Supports asynchronous networking, dynamic ports, and multi-threaded execution for efficient proxying.
